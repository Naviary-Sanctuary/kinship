import type { PartnerRelationship } from '../models/kinship';
import type { PedigreeId } from '../models/pedigree';

export const EMPTY_PEDIGREE_IDS: readonly PedigreeId[] = Object.freeze([]);
export const EMPTY_PARTNER_RELATIONSHIPS: readonly PartnerRelationship[] = Object.freeze([]);
